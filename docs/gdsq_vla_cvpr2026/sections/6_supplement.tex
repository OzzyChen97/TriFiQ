\section{Supplementary Evidence}
\label{sec:supplement}

\subsection{Ratio Sweep and Stability}

\input{tables/ratio_sweep}

The point-selection rule freezes 16:1 because it has the largest development task-macro SR. A separate post-freeze analysis resamples tasks and then paired seeds for 10,000 draws. No alternative ratio has a paired difference interval against 16:1 that excludes zero: for example, the 256:1 interval is $[-6.5,5.0]$ points and the CKA-only interval is $[-10.0,3.5]$ points. Leave-one-task-out chooses 16:1 for three omissions and ties 16:1 with 256:1 for the fourth. These diagnostics support stability within a plateau, not universal ratio optimality.

\subsection{Selection-Controlled GR00T Atomic Result}

\input{tables/primary14_results}

The four ratio-development tasks are excluded from this comparison. The final GR00T selector chooses the baseline path and is bitwise identical to the static \method row. It obtains 65.9\% SR on Primary-14, versus 45.6\% for QuantVLA W4A8; the paired difference is $+20.3$ points with interval $[13.9,26.9]$. FP16 obtains 73.9\%. Because QuantVLA and \method use different static byte totals, the result is a selection-controlled task comparison but not a fixed-budget causal comparison.

\subsection{Preregistered 50-Seed Component Ablations}

\input{tables/component_ablation}

The table is populated only after all five configurations reach exact Primary-14$\times$50 coverage and the five full-versus-ablation contrasts receive a joint Holm correction. Until then, its pending cells are claim-disabled. In the no-guards arm, a NaN, crash, or out-of-range action is retained as a formal failure rather than silently dropped.

\subsection{Earlier Five-Seed Diagnostic}

\input{tables/component_ablation_diagnostic}

This screen predates the 50-seed ratio freeze. Its overlapping task-cluster intervals permit only a qualitative observation that CKA and CS can be complementary. It does not substitute for the preregistered Primary-14$\times$50 component ablation containing CKA-only, CS-only, $w_i=1$, no guards, and no functional adjudication.

\subsection{Static Correction Ablation}

On the complete GR00T 50-task matrix, static ATM+OHB changes task-macro SR from 50.8\% to 49.4\%; the paired interval $[-3.28,0.52]$ points includes zero. On the complete $\pi_{0.5}$ static matrix, ATM+OHB changes 25.3\% to 26.6\%, while the preregistered held-out interval $[-0.39,3.13]$ and Holm-adjusted $p=0.542$ do not establish a gain. Neither combined configuration is an allowed output of the v8 selector. Standalone ATM and OHB remain static ablations; only the selector-chosen OHB runtime path populates the final row, and it does so through a separate complete 2,500-episode manifest.
