\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/dypac_dynamic_range_evidence.pdf}
  \caption{Evidence for per-forward A8 scaling. \textbf{(a)} Descriptive drift of calibrated per-channel ranges across flow steps. \textbf{(b)} Offline matched attribution relative to A16 under objective $J$ (log scale). \textbf{(c)} Under the otherwise identical $M_0$ runtime, the registered static table obtains 0/500 successes and dynamic A8 obtains 269/500. This comparison does not cover every possible static activation quantizer.}
  \label{fig:dynamic_range_evidence}
\end{figure}
