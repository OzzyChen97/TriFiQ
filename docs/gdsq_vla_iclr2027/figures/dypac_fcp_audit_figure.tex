\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{figures/dypac_fcp_audit.pdf}
  \caption{GR00T outcome of the abstaining full-policy audit. \textbf{(a)} Conservative FP16 benefits for all 116 valid one-layer counterfactuals; a positive value is required, but neither diagnostic is positive for any flip. \textbf{(b)} Each structured proposal has $J>0$ and fails at least one component-deviation guard; acceptance requires $J<0$ and all guards. \textbf{(c)} The audit therefore abstains and preserves the 100-W4/16-FP16 initializer. This finite-neighborhood decision is neither a separate measured success gain nor evidence of global optimality or physical safety.}
  \label{fig:fcp_audit}
\end{figure}
