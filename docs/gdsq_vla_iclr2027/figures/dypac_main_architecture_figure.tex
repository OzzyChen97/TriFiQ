\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/dypac_main_architecture.pdf}
  \caption{The full-context gap in VLA quantization. Conventional PTQ treats calibration data as fixed, but a quantized controller changes actions, future states, and the activation ranges on which later error depends. \method restores temporal context with a physical-sequence contract, whole-policy context with an abstaining acceptance audit, and deployment context with input-conditioned activation ranges. Dataset-specific audit outcomes are reported in Section~\ref{sec:attribution}.}
  \label{fig:pipeline}
\end{figure}
