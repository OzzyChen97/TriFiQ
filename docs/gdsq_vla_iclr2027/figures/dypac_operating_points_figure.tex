\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/dypac_operating_points.pdf}
  \caption{GR00T behavior--storage evidence on the 50-task RoboCasa365 target split. \textbf{(a)} Task-macro success versus exact static component storage (2,500 episodes per point); the dashed line connects non-dominated measured points and is not an interpolation. \textbf{(b)} Per-task differences for \method on matched tasks and seeds; boxes show median and interquartile range, and counts are better/tied/worse tasks. The protocol-matched $\pi_{0.5}$ rows are reported separately in Table~\ref{tab:main_results}.}
  \label{fig:operating_points}
\end{figure}
