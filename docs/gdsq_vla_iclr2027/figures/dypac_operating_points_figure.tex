\begin{figure}[t]
  \centering
  \includegraphics[width=0.94\textwidth]{figures/dypac_operating_points.pdf}
  \caption{GR00T behavior--storage evidence for selected measured configurations on the 50-task RoboCasa365 target split. \textbf{(a)} Task-macro success versus exact static-component storage (2,500 episodes per point). The dashed line connects displayed non-dominated points; it is not an interpolation. \textbf{(b)} Per-task differences for \method on matched tasks and seeds. Boxes show the median and interquartile range; counts denote better/tied/worse tasks. Table~\ref{tab:main_results} reports the complete comparison, including ActQuant and the protocol-matched $\pi_{0.5}$ results.}
  \label{fig:operating_points}
\end{figure}
