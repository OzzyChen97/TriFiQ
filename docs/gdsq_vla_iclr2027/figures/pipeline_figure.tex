\begin{figure}[t]
    \centering
    \setlength{\fboxsep}{5pt}
    \small
    \begin{tabular}{@{}p{0.285\linewidth}@{\hspace{3pt}}c@{\hspace{3pt}}p{0.285\linewidth}@{\hspace{3pt}}c@{\hspace{3pt}}p{0.285\linewidth}@{}}
    \fcolorbox{iclrblue}{blue!5}{\parbox{0.255\textwidth}{\centering\sloppy
        \textbf{1. $D_{\mathrm{PAC}}$ Evidence}\\[2pt]
        paired action chunks\\
        local + prefix drift\\
        pose + stitch + gripper\\
        mean + tail risk\\[2pt]
        \textbf{$D_{\mathrm{PAC}}$}}}
    & $\Longrightarrow$ &
    \fcolorbox{iclrblue}{green!6}{\parbox{0.255\textwidth}{\centering\sloppy
        \textbf{2. FCP Search}\\[2pt]
        precision flips\\
        exact byte budget\\
        full-policy score\\
        context safety\\[2pt]
        \textbf{frozen W4 / FP16 plan}}}
    & $\Longrightarrow$ &
    \fcolorbox{iclrblue}{orange!8}{\parbox{0.255\textwidth}{\centering\sloppy
        \textbf{3. DyRange Deploy}\\[2pt]
        Hessian group-64 W4\\
        dynamic channelwise A8\\
        one global plan\\
        no correction\\[2pt]
        \textbf{DyRange A8}}}
    \end{tabular}
    \caption{\method architecture. Precision evidence is computed on paired physical action sequences, candidate masks are judged as complete policies under an exact byte budget, and the frozen plan is deployed with dynamic activation ranges. On GR00T, full-context adjudication retains the initialized 100-W4/16-FP16 mask rather than claiming a newly discovered allocation.}
    \label{fig:pipeline}
\end{figure}
